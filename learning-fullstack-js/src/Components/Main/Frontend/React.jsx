export default function React(){
    return(
        <div className="card">
            <div className="card-head">
                <p className="step">08</p>
                <h2>React</h2>
            </div>
            <p>React builds your UI out of reusable components, written in JSX — a file format that lets you write HTML-like markup directly inside your JavaScript. You keep data in state, and when the state changes React updates the page for you, no manual DOM edits.</p>
            <div className="build">
                <span>Build</span>
                <p>Build a small video game store: a grid of games from your database, a detail page for each, and a cart you can add to and remove from. React frontend, Express API. No payments — the cart is enough.</p>
            </div>
        </div>
    )
}